#!/usr/bin/env node
/**
 * scripts/generate-og.mjs
 *
 * Spec: docs/07-folder-structure.md §1 — static OG images per route family
 * Implemented in: Milestone 6
 *
 * Generates public/og/*.png at build time. Must not pull in `sharp` — it is on
 * the forbidden list in docs/07 §3 (native bindings, no browser equivalent).
 */

console.log('generate-og: not implemented until Milestone 6');
